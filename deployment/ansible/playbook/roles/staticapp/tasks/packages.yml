---
- name: StaticApp | Packages | Install required system packages.
  apt: >
    pkg={{ item }}
    state=installed
    update-cache=yes
  with_items:
    - build-essential
  tags:
    - base
    - packages
    - provision
    - app
